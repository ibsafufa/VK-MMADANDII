const today=document.getElementById("today");const year=document.getElementById("year");const search=document.getElementById("search");const buttons=[...document.querySelectorAll(".meeting-button")];const now=new Date();today.textContent=now.toLocaleDateString("om-ET",{weekday:"long",year:"numeric",month:"long",day:"numeric"});year.textContent=now.getFullYear();search.addEventListener("input",()=>{const q=search.value.trim().toLowerCase();buttons.forEach(b=>{b.style.display=b.textContent.toLowerCase().includes(q)?"":"none"});document.querySelectorAll(".meeting-section").forEach(s=>{const visible=[...s.querySelectorAll(".meeting-button")].some(b=>b.style.display!=="none");s.style.display=visible||!q?"":"none"})});